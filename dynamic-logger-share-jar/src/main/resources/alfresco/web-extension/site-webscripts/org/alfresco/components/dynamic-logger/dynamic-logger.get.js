function main()
{
    var widget = {
        id : "DynamicLogger",
        name : "Alfresco.DynamicLogger",
        options: {
            loggerName: "loggerName",
            loggerLevel: "loggerLevel"
        }
    };
    model.widgets = [widget];
    
}

main();